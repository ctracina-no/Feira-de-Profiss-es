/** @type {import('tailwindcss').Config} */
module.exports = {
  // Ajuste esses caminhos se sua estrutura de pastas mudar.
  // O Tailwind CLI só gera classes que encontra literalmente nesses arquivos.
  content: ["./html/**/*.html", "./js/**/*.js"],
  theme: {
    extend: {
      fontFamily: {
        sans: ["'Public Sans'", "-apple-system", "Segoe UI", "Roboto", "sans-serif"]
      },
      colors: {
        ifsp: {
          escuro: "#0b4a2e",
          verde: "#1c7a4a",
          suave: "#e7f3ec",
          dourado: "#e8a13d",
          bg: "#f6f7f2",
          texto: "#17251d",
          textoSuave: "#51625a",
          borda: "#dbe6de",
          erro: "#b3402f"
        }
      },
      boxShadow: {
        card: "0 8px 24px rgba(11,74,46,0.10)"
      },
      keyframes: {
        findPath: {
          "0%": { transform: "rotate(-140deg)" },
          "60%": { transform: "rotate(35deg)" },
          "80%": { transform: "rotate(-8deg)" },
          "100%": { transform: "rotate(0deg)" }
        }
      },
      animation: {
        findPath: "findPath 2.4s cubic-bezier(.4,0,.2,1) 1"
      }
    }
  },
  plugins: []
};